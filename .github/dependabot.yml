version: 2
updates:
  # The package itself has no third-party SwiftPM dependencies, so only the
  # GitHub Actions used by CI and the upstream-sync workflow are tracked here.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
